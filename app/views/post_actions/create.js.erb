html_id = '#comment-list-' + <%= @feed_item.id %>;
$("<%= escape_javascript(render 'shared/comment', :comment => @action) %>").appendTo(html_id).hide().show();
$('#new_comment #comment_content').val("");


