<% provide(:title, "All users") %>

<h1>User List</h1>



<ul class="users">
	<% @users.each do |user| %>
    	<li>
	  		<%= avatar_for(user) %>
	  		<%= link_to user.name, user %><br />
	  		<%= friendship_link(user) %>
		</li>
  	<% end %>
</ul>